{
  "name": "pi-bloccs",
  "version": "1.0.0",
  "description": "Pi approximation through block collisions simulation",
  "main": "index.js",
  "scripts": {
    "vercel-build": "cd pi-collision-react && npm install --no-fund --no-audit --loglevel=error && npm run build",
    "start": "node index.js",
    "build": "cd pi-collision-react && npm install --no-fund --no-audit --loglevel=error && npm run build",
    "start-mcp": "node mcp-vercel.js"
  },
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.9",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  }
} 
